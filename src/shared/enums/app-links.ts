export enum AppLinks {
  LOGIN = "/login",
  USERS = "/users",
  NEWS = "/news",
  NEWS_NEW = "/news/new",
  COLLECTIONS = "/collections",
  EXHIBITIONS = "/exhibitions",
  TICKETS = "/tickets"
}